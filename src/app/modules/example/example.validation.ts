
import { z } from "zod";

const createExampleSchema = z.object({
	
});
const updateExampleSchema = z.object({
	
});

export const ExampleValidations = {
	createExampleSchema,
    updateExampleSchema,
};

